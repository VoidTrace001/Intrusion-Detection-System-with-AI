# Quantum AI-IDS: Advanced Cyber Defense Platform
**Technical Documentation & Operational Manual**

---

## 1. Executive Summary
The **Quantum AI-IDS** is a military-grade Intrusion Detection System (IDS) designed to detect, analyze, and neutralize advanced cyber threats in real-time. Unlike traditional signature-based systems, it leverages **Machine Learning (Random Forest)**, **Deep Learning (Autoencoders)**, and **Global Threat Intelligence (OSINT)** to identify both known attacks and zero-day anomalies.

---

## 2. System Architecture
The system operates on a modular "Brain-Body" architecture:

### **A. The Neural Core (Backend)**
- **Packet Sniffer:** Captures raw network traffic using `Scapy` (Python).
- **Preprocessing Engine:** Converts raw bytes into statistical vectors (Flow Duration, Protocol, Flags).
- **Inference Engine:**
    - **Model A (Classifier):** Identifies attack types (DoS, Probe, Brute Force).
    - **Model B (Anomaly Detector):** A PyTorch Autoencoder that flags "Zero-Day" threats based on reconstruction error.
- **Active Defense:**
    - **Firewall Manager:** Executes `netsh` commands to block IPs instantly.
    - **Honeypot:** Spins up "Ghost Ports" (22, 3306) to trap attackers.

### **B. The Tactical Interface (Frontend)**
- **Visuals:** A React-based "Cyber Command" dashboard with 3D Globe visualization.
- **Real-Time Feed:** A 10Hz WebSocket stream displaying every packet interception.
- **Forensic Tools:** Deep Packet Inspection (Hex View) and AI Reasoning modules.

---

## 3. How It Works (The Logic Flow)

1.  ** interception:** A packet hits the network card.
2.  **Extraction:** The system extracts 12 key features (Source IP, Length, Flags, etc.).
3.  **Intelligence Fusion:**
    - The **ML Model** calculates a base probability.
    - The **OSINT Module** checks the IP against global reputation databases.
    - The **XAI Engine** generates a human-readable reason for the flag.
4.  **Risk Scoring:**
    - Formula: `(Base_Weight * Confidence) + Reputation_Modifier`
    - Result: A score from 0.0 to 10.0.
5.  **Response:**
    - **Score > 8.0:** Auto-Block via Firewall.
    - **Score > 5.0:** Alert via Telegram SOC.
    - **All:** Logged to the Immutable Blockchain Ledger.

---

## 4. Cybersecurity Value
This system addresses critical gaps in modern defense:

- **Zero-Day Protection:** Traditional antiviruses fail against new malware. Our Autoencoder detects them by behavior, not signature.
- **Speed:** The "10Hz Uplink" ensures reaction times faster than humanly possible.
- **Forensic Readiness:** By capturing raw Hex payloads and generating "Evidence Reports," it aids in legal prosecution and incident analysis.
- **Deception Warfare:** The Honeypot module wastes attacker time and resources, turning the defense into an offense.

---

## 5. Deployment
- **Backend:** `python run.py` (FastAPI + Scapy)
- **Frontend:** `npm start` (React + Tailwind)
- **Defense Level:** Adjustable via the "DEFCON" protocol (1-5).

*Generated by Quantum AI Architecture*
